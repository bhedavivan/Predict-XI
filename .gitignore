# Secrets — never commit your API token
.env

# Python
__pycache__/
*.pyc

# Editor / tooling
.claude/
.vscode/
.idea/

# Large / regenerable data (comments must be on their own line in .gitignore)
# processed_data.json = full training matrix (big), regenerated by training
processed_data.json
# matches_data.json = API-path processed cache
matches_data.json

# Committed on purpose so the app works out-of-the-box:
#   model.json, model_metrics.json, team_stats.json
